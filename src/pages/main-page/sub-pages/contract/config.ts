import { SubPageProp } from '@core/types';

import { config as configManual } from '../manual/config';

export const config: SubPageProp = {
  title: 'Договор оферты',
  paragraph: configManual.paragraph,
};
